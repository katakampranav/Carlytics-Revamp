"use client";

import { motion } from "framer-motion";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";

interface ReportSectionProps {
  report: string;
}

export default function ReportSection({ report }: ReportSectionProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ delay: 0.3 }}
      className="bg-white border border-zinc-200 rounded-3xl p-8 shadow-sm"
    >
      <div className="mb-6">
        <h2 className="text-xl font-bold text-zinc-900">AI Valuation Report</h2>
        <p className="text-sm text-zinc-500 mt-0.5">Generated by our expert AI valuation model</p>
      </div>

      <div className="prose prose-sm max-w-none prose-zinc prose-headings:font-bold prose-strong:text-zinc-900 prose-li:text-zinc-700">
        <ReactMarkdown remarkPlugins={[remarkGfm]}>
          {report}
        </ReactMarkdown>
      </div>

      <div className="mt-8 pt-6 border-t border-zinc-100 flex items-center gap-2">
        <div className="w-2 h-2 rounded-full bg-emerald-400" />
        <p className="text-xs text-zinc-400">
          AI-generated report. Use as a guide — actual market prices may vary by region and negotiation.
        </p>
      </div>
    </motion.div>
  );
}
